'use strict';

/**
 * Объект с настройками игры.
 * @property {number} rowsCount Количество строк в карте.
 * @property {number} colsCount Количество колонок в карте.
 * @property {number} step Размер одного шага.
 */
const config = {
    rowsCount: 10,
    colsCount: 10,
    step: 1
};
